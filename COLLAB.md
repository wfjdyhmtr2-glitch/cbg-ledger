# 藏宝阁台账 · 跨电脑协作指南

> 给另一台电脑上的 WorkBuddy 助手 / 硕哥：这份文件说明项目全貌、口径约定与协作规则。改代码前先读完。

## 项目是什么

梦幻西游藏宝阁倒卖与固定资产管理的私有后台。**Vue 3（本地 vendor，无构建）+ Supabase（Postgres + Auth）**，纯静态、零依赖、双击 index.html 都能跑（但要登录）。

- 云端数据库：Supabase 项目 `hykwkzeoqcenrqnjccsi`，连接信息已预置在 `index.html`（公开密钥，安全）
- **数据只有一份**（在 Supabase），代码靠本 Git 仓库在两台电脑间同步

## 目录结构（改哪找哪）

| 路径 | 职责 |
|---|---|
| `src/core/model.js` | **数据模型**：实体工厂、分类字典（一级/二级）、区服字典、时间锁计算 |
| `src/core/compute.js` | **核算引擎**：成本分摊、盈亏三口径、汇总。改这里必跑回归 |
| `src/core/fee.js` | 信息费/到手价规则 |
| `src/core/store.js` | 状态管理 + Supabase 读写 |
| `src/core/zones-data.js` | 梦幻全服 1049 台区服字典 |
| `src/adapters/supabase.js` | 行 ↔ 对象映射（新增字段必改 `toRow`，读取整行展开不用改） |
| `src/views/*.js` | 页面：总览/角色/商品/区服/分析/固定资产/价值计算/设置 |
| `src/components/forms.js` | 所有录入表单（含固定资产售出弹窗） |
| `supabase/schema.sql` | 建表脚本，**幂等可重复执行**，加字段后重跑即可 |
| `supabase/migration-*.sql` | 只含新字段的增量脚本，给已有库用（如 `migration-fixed-asset-sold.sql`） |
| `test/*` | 四组自测，见下 |

## 改动必做

1. **加了数据字段**：四处同步 —— `model.js` 工厂、`supabase.js` 的 `toRow`、
   `forms.js` 表单、`schema.sql`（建表 + `alter ... add column if not exists`），
   然后让硕哥在 Supabase 跑一遍（整份 schema.sql 重跑是安全的，或给一份增量脚本）
2. **改了核算口径**：跑 `npm test`，断言口径变了就同步改测试
3. **模板里别调用未暴露的函数**：Vue 模板只能用 methods/computed/全局 mixin 暴露的东西，
   模块导入的函数在模板里是 undefined（会白屏）—— `tpl-check.mjs` 会抓这个
4. **验证**：`npm test` 四组全绿，再用 `?demo=1` 上浏览器实测数字

## 账目口径（全模块统一）

- **实际盈亏** = 已售资产到手 − 成本（落袋，不变）
- **预计盈亏** = 在手资产估值 − 成本（随上架价波动）
- **总盈亏** = 实际 + 预计
- **估值**：挂了牌的按上架价（毛值），没挂的按买入成本；空壳号没挂价按 0
- **成本归集**：总成本 = 角色买入价 + 商品自填买入价（两者**相加**）。
  拆号时商品填 0，就等于成本全在角色上
- **固定资产**不进倒卖盈亏；但**标了已售的**会进「分析 → 固定资产流出」单独出账
- 「固定资产流出」有两条来源：固定资产页点「售出」（主路径，只标记可撤销）、
  商品表单里手工勾「固定资产流出」（老路径 / 手工补录）

## 测试（改完必跑）

```bash
npm test    # verify + assets-sell + imports + tpl-check
```

| 脚本 | 查什么 |
|---|---|
| `test/verify.js` | 核算恒等式、手续费规则、成本分摊守恒、区服加总（51 项） |
| `test/assets-sell.mjs` | 固定资产售出：标记不删、可撤销、费率、不进倒卖账（14 项） |
| `test/imports.js` | 无裸模块名、无循环依赖 |
| `test/tpl-check.mjs` | 重复顶层选项键、模板调用了未暴露的函数 |

后两组专治「自测全绿、一开浏览器整页空白」。
`?demo=1` 可免登录看演示数据（改动只在内存，刷新还原）。

## 协作规则（两台电脑）

1. **改前先 pull，改后立刻 push**——commit message 写清楚改了什么
2. 改完跑 `npm test`，全绿再 push
3. **公网地址是部署快照**，代码合并后要在主开发机重新发版才生效
4. 同一文件两边都要改时，先沟通分工，避免合并冲突

## 本机发布通道

`scripts/sync-github.py` 是主开发机（这台 Mac）专用的发布脚本——该机网络对 git 推送协议有干扰，
改完代码后由助手通过它把全量内容镜像到本仓库（走 GitHub API）。另一台电脑正常 git pull 即可拿到。

## 版本历史

- 2026-09-17 v1：倒卖核算 + 固定资产 + 分析 + 跨服时间锁 + 二级分类 + 上架/售出流程 + 实际/预计盈亏区分
- 2026-09-18 v2：固定资产可售出（**只标记已售、保留记录、可撤销**，已售独立成「已售」区）；
  成本口径定为「角色买入价 + 商品自填买入价」相加；分析页「固定资产流出」合并已售固定资产；
  新增 `tpl-check` / `imports` / `assets-sell` 三组自测；README 与本文档同步更新
